import React from 'react';
import { View } from 'react-native';
import styles from './styles';

interface AlbumCategoryProps {

}

export const AlbumCategory: React.FC<AlbumCategoryProps> = ({}) => {
        return (
            <View style={styles.container}>jjg</View>
        );
}